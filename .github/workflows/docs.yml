name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "scripts/sync-roadmap.sh"
      - "README.md"
      - "ROADMAP.md"
      - "Makefile"
      - ".github/workflows/docs.yml"
      - "**/*.go"
  pull_request:
    paths:
      - "docs/**"
      - "scripts/sync-roadmap.sh"
      - "README.md"
      - "ROADMAP.md"
      - "Makefile"
      - ".github/workflows/docs.yml"
      - "**/*.go"
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

# Required by actions/deploy-pages. `contents: read` is enough for the
# checkout; `pages: write` and `id-token: write` are what the official
# Pages deploy action expects. Deploy runs on push to `main` and
# workflow_dispatch only (pull requests still build without uploading).
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment per ref. Don't cancel in-progress
# deploys on `main` so a queued release isn't lost; PR builds key on the
# PR ref so they can run in parallel with main.
concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # VitePress `lastUpdated` reads commit history from git.
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install docs dependencies
        working-directory: docs
        run: bun install --frozen-lockfile

      - name: Generate roadmap mirror for docs
        run: make docs-content

      - name: Build site
        working-directory: docs
        run: bun run build

      - name: Verify build output
        run: |
          set -euo pipefail
          test -s docs/.vitepress/dist/index.html
          file_count=$(find docs/.vitepress/dist -type f | wc -l | tr -d ' ')
          echo "✓ docs/.vitepress/dist looks healthy (${file_count} files)"

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs/.vitepress/dist

  deploy:
    name: Deploy to GitHub Pages
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: Docs
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
